<h1><%= @article.title %></h1>
<p><%= @article.content %></p>
<%= link_to 'Modifier cet article', edit_article_path(@article.id) %>
<%= link_to 'Supprimer cet article', article_path(@article.id), method: :delete, data: {confirm: "Are you sure?"} %>
